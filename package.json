{
  "name": "relate-workspace",
  "private": true,
  "packageManager": "pnpm@9.0.6",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev",
    "release": "bash scripts/release.sh"
  },
  "devDependencies": {
    "miniflare": "^4.20260317.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
