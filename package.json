{
  "name": "crm-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "miniflare": "^4.20260317.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
